import { Container, Typography } from "@mui/material";

function Wishlist() {
    return (
        <Container>
            <Typography variant="h4">
                Wishlist Page
            </Typography>
        </Container>
    );
}

export default Wishlist;